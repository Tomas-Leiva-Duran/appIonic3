import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'my.app_ionic3',
  appName: 'my-app_ionic3',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
